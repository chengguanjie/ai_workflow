{
  "build_command": "pnpm install --frozen-lockfile && npx prisma generate && pnpm build",
  "start_command": "npx prisma db push --skip-generate && npx next start -p ${PORT:-3000} -H 0.0.0.0",
  "serverless": false
}
